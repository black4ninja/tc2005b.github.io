export {
  motorPorOmision as motorPorOmisionDeTipo,
  motoresDe as motoresDeTipo,
  plantilla as plantillaDiagrama,
} from '@tc2005b/diagramas-catalogo/plantillas';

/**
 * Esqueletos de arranque, separados de `etiquetas.ts` a propósito.
 *
 * Importar cualquier cosa de aquí arrastra la tabla entera de plantillas —una
 * por tipo y motor—, y solo la necesitan las pantallas que ESCRIBEN diagramas:
 * el taller y el editor de autoría. Las de listado, que solo pintan un rótulo,
 * se quedan con `etiquetas.ts`.
 */
